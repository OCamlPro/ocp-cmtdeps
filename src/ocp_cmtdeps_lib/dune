; generated by drom from package skeleton 'library'

(library
  (name ocp_cmtdeps_lib)
  (public_name ocp_cmtdeps_lib)
  (wrapped true)
  (libraries compiler-libs.common ez_toposort ez_file ez_dot )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package ocp_cmtdeps_lib))


